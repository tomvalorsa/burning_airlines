<!DOCTYPE html>
<html>
  <head>
    <title>Burning Airlines</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>

    <meta content='<%= @current_user.present? ? @current_user.id : "" %>' name='user-id'/>

 <!-- <body class='<%= @current_user.present? ? "logged_in" : "logged_out" %>'> -->
  </head>
  <body>
    <header>
      <h1 >Burning Airlines <i class="fa fa-plane"></i><i class="fa fa-fire"></i></h1>
      <nav class="main-nav">
        <ul>
        <%== nav_menu %>
        </ul>
      </nav>
    </header>


  <p id="current-user-id" style="color:white"><%= @current_user.id %></p>
  <%= yield %>

  </body>
</html>
